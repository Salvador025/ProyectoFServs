<div>
    {{> navbar}}
    <div class="body">
        <div class="form-header">
            <h1>Subir Nuevo Tablero</h1>
        </div>
        <div class="upload-form">
            <form
				action="/submit-your-form-handler"
				method="POST"
				enctype="multipart/form-data"
                id="createBoardForm"
			>
				<label for="boardName">Nombre del Tablero:</label>
				<input type="text" id="boardName" name="boardName" required />

				<label for="description">Descripción:</label>
				<textarea
					id="description"
					name="description"
					required
					placeholder="Escribe aquí la descripción del tablero..."
				></textarea>

				<label for="file-upload" class="file-upload">
					<div class="drop-area">
						Arrastra y suelta una imagen aquí o clic para seleccionar archivo
					</div>
					<input
						type="file"
						id="file-upload"
						name="Board"
						accept=".jpg, .jpeg, .png, .webp"
						style="display: none"
						required
					/>
				</label>

				<button type="submit" class="submit-btn">Subir Tablero</button>
			</form>
        </div>
    </div>
    {{> footer}}
</div>

<div
    class="modal fade"
    id="creatorModal"
    tabindex="-1"
    role="dialog"
    aria-labelledby="modalLabel"
    aria-hidden="true"
>
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalLabel">Hazte Creador</h5>
                <button
                    type="button"
                    class="close"
                    data-dismiss="modal"
                    aria-label="Close"
                    id="closeModal"
                >
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                ¿Quieres convertirte en un creador y comenzar a subir tus propios
                tableros?
            </div>
            <div class="modal-footer">
                <button
                    type="button"
                    class="btn btn-secondary"
                    data-dismiss="modal"
                    id="noThanks"
                >
                    No gracias
                </button>
                <button type="button" class="btn btn-primary" data-dismiss="modal" id="becomeCreator">
                    Hazte Creador
                </button>
            </div>
        </div>
    </div>
</div>